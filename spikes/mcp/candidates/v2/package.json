{
  "name": "spendbar-mcp-candidate-v2",
  "private": true,
  "description": "T-009 candidate workspace: MCP SDK v2. Exact experimental pins - reproducible evidence needs a fixed resolution; the root production pinning syntax is open question 2 and is NOT decided here.",
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "4.4.3"
  }
}
