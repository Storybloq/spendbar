{
  "name": "spendbar",
  "version": "0.1.0-dev",
  "description": "Per-project Claude Code + Codex spend tracking — CLI, MCP server, and native macOS menubar.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "spendbar": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=22.12.0"
  },
  "keywords": [
    "claude-code",
    "codex",
    "usage",
    "cost-tracking",
    "menubar",
    "mcp"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test tests-ts/",
    "parity": "node tests-ts/parity.mjs",
    "spike:rounding": "node spikes/rounding/spike.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
