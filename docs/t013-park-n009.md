# T-013 — plan assessment: the ticket repair landed; the ticket then blocks itself

**First act completed: T-013's ticket text repaired at eight sites** (ISS-086, now resolved). The
reader computation lease that N-007 decision 4 CUT was still live text throughout — scope bullet,
three failure-mode rows, AC 6, risks, the T-008 dependency entry, and an open question about its
deadline values — with the amendment appended only at the bottom, so a top-down reader would have
implemented a cut feature. All eight repaired; the cut is now stated at every point of use, and
"reader-side computation" is named in Scope — out so it cannot return as "just an optimization".

That repair is real, durable work and it stands regardless of what follows.

---

## Why a plan cannot be written now

**AC 4 declares its own blocker, and both dependencies are parked as of this session.**
The ticket states, at the AC: *"Consumes T-025's `readGeneration(id)` and T-011's pin-request path.
BOTH ARE PARKED. This AC must not be attempted without them."* That is not my inference — it is the
filing's own instruction, and I verified both parks in this same session:

- **T-025** — parked on ISS-090 (surface 6 cannot reproduce `cmdHourly` from one all-time pass) and
  ISS-091 (no witnessable disappearance policy for `claude-blocks`). `readGeneration` is item 2 of
  that ticket; review confirmed it is independently landable, but a parked ticket does not land its
  subset automatically.
- **T-011** — parked because AC 1 cannot honestly publish: `Provenance.ccusageFetchedAt` is a
  required scalar (`types.ts:294`) with no honest value (ISS-077), and the fix is T-025 item 5. The
  pin-request path is T-011's to build, and this session's review additionally found that my design
  for it did **not** close ISS-083 — manifest membership proves an id is *referenced*, not that the
  artifact is present or readable.

So AC 4 has no path, and the cursor contract is not a peripheral AC — it is what makes every paged
tool answer consistent.

**AMENDED 2026-08-05 — THE AC 4 LEG ABOVE IS DISCHARGED. T-013 STAYS PARKED, on the reasons that
follow, not on this one.** Kept rather than rewritten: the reasoning is what produced the owner
action. Commit `9524902` (ticket **T-026**, complete) did exactly the split the last section of this
record asks for, so both bullets above are now false about the tree:

- **T-025's item 2 did land.** `readGeneration(fs, paths, id, attempts = 3)` is at
  `src/snapshot/store.ts:2806`; its own doc comment names *"T-025 item 2, T-013 AC 4"* as what it is
  for (`:2781-2782`). T-025 itself remains parked on ISS-090/ISS-091 — the registry, the coverage
  policies and `SnapshotPayloadV1` — so "a parked ticket does not land its subset automatically" was
  right; the owner landed it deliberately.
- **T-011 is UNPARKED** (2026-08-05). `Provenance.ccusageFetchedAt` no longer exists: the required
  field is `ccusageInvokedAt` (`src/snapshot/types.ts:301`, enforced `store.ts:960`, `:1077`,
  `:1083`), which the writer proves from its own clock, and **ISS-077 is `resolved`**. The
  pin-request path is still T-011's to *build* — unparking authorizes the work, it does not perform
  it — so AC 4 still has no *shipped* producer, which is the distinction to carry forward instead of
  "parked".

**Two obligations survive intact and are not touched by any of this.** ISS-083 stays **open**:
`createPin` still accepts any `generationId`, so no store operation validates-then-pins atomically,
and `readGeneration` cannot authorize a not-yet-retained generation before the pin that would
authorize it exists — the circularity this record's own §2 diagnosis names. And AC 4 must be written
against two shipped properties of `readGeneration`: it evaluates **no** `pin.until` and takes no
clock (`store.ts:2792-2796`, `:2806-2811`), so an expired-but-unswept pin still serves and expiry is
`cursor.ts`'s to hold; and `gone` is a **disposition, not a diagnosis** — absent and unusable both
return it (`store.ts:2909`, `:2920-2924`).

**The other park reasons below are UNAFFECTED and still govern**: ISS-089 (`usage_blocks` payload
contract), ISS-088 (AC 2 has nothing to replay), and `SnapshotPayloadV1` existing as a decision
rather than an importable type because T-025 is parked. T-013 remains correctly parked.

**ISS-089 is unresolved and is now harder, not easier.** Open question 4 is the `usage_blocks`
payload contract. ISS-023/ISS-026 are *resolved* and name this ticket: "no validator lands in
T-013". But every reader needs a payload schema and `blocks[]` is not uniform, so a strict
constructor **is** runtime `CCUSAGE_CMD` validation. N-009's two-stage compat/normalize boundary was
the obvious escape — and it was **rejected in T-025 review round 2**: stage 1 cannot turn a tolerated
non-gap block with missing timestamps, `isActive: null` or absent `entries` into a strict row without
inventing values, dropping a row the CLI displays, or making stage 2 refuse the publish. The conflict
therefore stands with its escape route closed.

**AC 2 cannot be discharged as worded (ISS-088).** It requires conformance "against recorded
real-client transcripts", but T-009's real-client evidence is counters and SHA-256 digests plus a
three-entry frame summary over a `spendbar_probe` tool — roughly 4.4 KB with **no replayable frame
bodies**. There is nothing to replay. This needs an owner amendment.

---

## What N-009 did and did not fix here

**Fixed: ISS-087.** The payload fact grain — the reason T-013's five tools had no proven source
shape — is decided. `generation.payload` gets `SnapshotPayloadV1`: per-surface normalized row tables
plus producer totals, canonically ordered, behind one branded constructor. That was a critical park
reason last session and it is answered.

**Not fixed: the implementation.** T-025 builds it and is parked, so the schema exists as a decision
and not as a type any reader can import. T-013 cannot be written against a decision.

---

## Disposition

Park. The criterion met is the same one T-025 met — **a scope item that cannot be sound in isolation**
— and here the filing says so itself at AC 4. Three independent owner actions are required:

1. Land T-025 items 2 and 5 (the split the reviewer already confirmed is sound). This unblocks
   `readGeneration` for AC 4 *and* unblocks T-011's AC 1, which unblocks the pin-request path.
2. Rule on **ISS-089** — the CCUSAGE_CMD/payload-schema conflict, now that the two-stage escape is
   closed.
3. Amend **AC 2** per ISS-088, or authorize capturing replayable transcripts.

Nothing in T-013 is blocked on *design* work I could do instead. The five tools, the input schemas,
the timezone rule, `--cached`, the token budget and the honest-stale path are all specifiable — but
they sit behind a cursor contract with no dependencies and a payload schema with no implementation,
and planning them in isolation would produce exactly the "plan written around a contradiction" this
project keeps rejecting.

---

## Review round 1 — park CONFIRMED, and four defects in my own repair

Reviewer's verdict, verbatim: **"Park is correct; the three parks are causally connected, and no
T-013 subset provides a coherent public surface while the five-tool payload contract remains
blocked."** It answered my over-parking question directly: **not over-parking.** It also confirmed
that none of the candidates I considered splitting out (input schemas, timezone rule, `--cached`,
token budget, honest-stale path) is worth shipping alone.

Four findings, all valid, **all of them defects I introduced in the repair itself**, all now fixed:

1. **The ninth stale reference was the TICKET TITLE** — "5 bounded tools + *reader computation
   lease* + --cached". The highest-visibility instruction in the filing, contradicting the repaired
   scope. I fixed eight body sites and missed the title: the class-and-siblings pattern, *inside the
   repair meant to fix it*. Renamed to "MCP server: 5 bounded snapshot tools + --cached".
2. **I materially weakened AC 2 while claiming I had only "recorded" it.** The transcript requirement
   was moved out of the operative sentence into a parenthetical, so a reader could discharge the
   shortened AC with no owner authorization. Weakening an AC is an owner's act. Restored to the
   operative sentence and marked HELD pending ISS-088, with the scripted conformance suite named as
   independently landable *preparation* that explicitly does not discharge it.
3. **I closed ISS-086 without answering its real question.** Repeating N-007 #4's "actionable
   service-not-running signal" supplies **authority** for the contract but names no **caller** that
   can honestly know machine-wide service absence — while the ticket listed T-011, owner of the only
   prospective probe, as "otherwise not a blocker". That is L-006 violated inside my own repair.
   **ISS-086 reopened**, narrowed to carry only the witness question: the contract weakens to a
   witnessable `undetermined`, the true status is HELD on T-011's probe, and asserting "not running"
   on evidence this ticket can obtain is recorded as a defect. Three owner options costed.
4. **ISS-074 omitted from the prerequisites.** T-013 forbids lifecycle scripts while the root
   manifest still declares `prepack` **and** `prepare`, with no contract verifying the transitive
   lifecycle/native closure — so even an adapter-only subset cannot merge honestly, and those
   scripts' packaging guarantees must be **replaced**, not merely deleted. Added as a hard
   prerequisite.

The owner-action list therefore grows from three to four: land T-025 items 2+5; rule on ISS-089;
amend AC 2 per ISS-088; **and** settle ISS-074 with its replacement obligation. ISS-086's witness
question is a fifth, though (a) is shippable the moment it is ratified.

## Preserved

Ticket repaired at eight sites; **ISS-086 resolved** (including the `serviceStatus` ratification it
requested — N-007 decision 4's own "actionable service-not-running signal" is the authority, now
recorded at the point of use in AC 5 and the scope bullet). ISS-089 updated with the fact that the
two-stage boundary was rejected. AC 2's and AC 4's blockers are now stated at the point of use rather
than in a footnote, so the next reader meets them where they bite.
