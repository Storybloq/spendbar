{
  "components": [
    "snapshot",
    "ccusage"
  ],
  "createdBy": "T-025 plan round 3 (codex reviewer, verified)",
  "dedupeKey": "t025-plan-r3:sourceversion-key-domain",
  "discoveredDate": "2026-08-04",
  "displayId": "ISS-082",
  "id": "ISS-082",
  "impact": "THIS IS THE PRIOR QUESTION TO ISS-081, and it went unasked through three plan-review rounds because I kept saying \"per-source\" without ever defining a source.\n\nTHE GAP. `sourceVersion` is `Record<string, number>` (types.ts:303). NOTHING in the codebase binds what the keys MEAN. `compareSourceVersions` (dominance.ts:161-190) treats them as opaque strings. The shipped tests use provider-like keys - `{claude: 10}`, `{claude: 20, gemini: 1}`, `{a: 5, b: 9}` - while the only per-entity ccusage surface, `claude daily --instances` (ccusage.ts:68-78), is per PROJECT. N-007 decision 1 and N-008 1a both reason about \"sources\" throughout and neither defines one. N-008 1a's own example (\"codex vanishing while claude advances\") is provider-shaped.\n\nWHY IT IS LOAD-BEARING RATHER THAN PEDANTIC. The answer changes what evidence exists, and therefore changes ISS-081's conclusion:\n\n- If a source is a PROVIDER (claude, codex): `ccusage blocks --json` is a Claude-only command, so its `actualEndTime` (ccusage.ts:96, tests/fake_ccusage.py:67-70) is a PROVIDER-LEVEL sub-day activity signal. Claude then HAS a per-source sub-day witness and only Codex lacks one. ISS-081 shrinks to a Codex-shaped problem.\n- If a source is a PROJECT or instance: `blocks` carries no project key at all, so its timestamp is global and cannot witness any single source. ISS-081 stands at full width.\n- If a source is a TRANSCRIPT ROOT or something else again: neither analysis has been done.\n\nSo \"no per-source sub-day signal exists\" is NOT ESTABLISHED, and cannot be until this is answered. I asserted it for two revisions of the T-025 plan on an undefined term.\n\nSECOND-ORDER CONSEQUENCES the decision also settles, each of which some parked ticket needs:\n- What N-008 1a's retirement attestation is ABOUT. Retiring a provider (\"I stopped using codex\") and retiring a project (\"I deleted that repo\") are different events with different witnesses and wildly different frequencies. Project churn is routine; provider churn is rare. A retirement mechanism sized for one is wrong for the other.\n- How large the key set gets, and therefore how often the one-sided-key wedge fires at all. A handful of providers is stable. Projects appear and vanish constantly, which makes both wedge directions ordinary rather than exceptional.\n- What `provenance.sourceTimestamps` (store.ts:1064-1073) is keyed by, since it is presumably the same domain and is validated today without one.\n\nWHAT IS NEEDED. An owner decision naming the key domain, and stating it where the store can be checked against it. It should be answered BEFORE ISS-081, because it changes ISS-081's answer rather than merely informing it.\n\nRECOMMENDATION: provider-level keys. It matches every shipped test fixture, it matches N-008 1a's own worked example, it keeps the key set small and stable (which makes both one-sided-key wedges rare rather than routine), and it is the only choice under which a sub-day witness demonstrably already exists for at least one provider. Costs: it gives up per-project regression detection, so a single project's data going backwards inside an advancing provider would not refuse - which should be stated as accepted rather than discovered later.\n\n---\n\n## ADDED 2026-08-04, from T-011 re-plan review round 2 (codex, 00000000-0000-4000-8000-000000000000)\n\nTHIS ISSUE BLOCKS A T-011 SCOPE ITEM DIRECTLY, not only T-025's. I claimed in the T-011 re-plan that ISS-082/081 \"have no consequence for T-011\" because the base plan was written to be sound either way. That claim is FALSE and the review refuted it:\n\n- N-008 1a assigns the retirement ATTESTATION to the service - \"The service attests retirement only after verifying the INPUT side (the source's transcript location exists and is empty/pruned)\". The service is T-011. Until the key domain is decided, T-011 cannot know what entity is being retired, where its declaration lives, or which declared value to pass to `publishSnapshot.retireSources`. There is no honest caller, which is an L-006 witness failure.\n- It also reaches the generic bootstrap-REPLACEMENT operation the base plan ships in T-011 (docs/t011-headless-service-plan.md:1148). That operation resets the store and publishes with `live: null`, bypassing dominance entirely, so its safety rests on the candidate being semantically truthful rather than merely structurally valid. No caller can supply a semantically truthful non-empty `sourceVersion` while this is open.\n\nT-011 remains implementable at a reduction (every generation it publishes carries `sourceVersion = {}`, so an empty key set cannot shrink and the retirement path is unreachable rather than merely unimplemented; the replacement operation is held behind a test-only seam). But that reduction is a CONSEQUENCE of this issue and must be recorded as such, not presented as unrelated.",
  "location": [
    "src/snapshot/types.ts:303",
    "src/snapshot/dominance.ts:161-190",
    "src/ccusage.ts:68-78",
    "src/ccusage.ts:96"
  ],
  "phase": null,
  "relatedTickets": [
    "T-011",
    "T-012",
    "T-013",
    "T-025"
  ],
  "resolution": "RESOLVED 2026-08-05 \u2014 ANSWERED by N-009 (.story/notes/N-009.json) via docs/snapshot-facts-and-watermarks.md v2 \u00a71, which names this issue explicitly: \"\u00a71 A source is an INGESTION SURFACE (answers ISS-082)\" (line 14).\n\nTHE ANSWER. The key domain is none of the three candidates this issue posed (provider / project / transcript root). A source is an INGESTION SURFACE \u2014 the six inputs the service consumes, enumerated at doc lines 19-24: `claude-instances` (ccusage claude daily --instances --breakdown --json, ccusage.ts:73), `all-daily` (ccusage daily --breakdown --json, ccusage.ts:87), `claude-blocks` (ccusage blocks --json, ccusage.ts:97), `codex-sessions` (ccusage codex session --json, ccusage.ts:104), `codex-daily` (ccusage codex daily --json, ccusage.ts:118), `claude-transcript-hours` (writer-owned transcript scan into half-hour buckets). The definition is not prose-only: \u00a71 requires an executable ingestion-surface registry, fixed per SCHEMA_VERSION, with sourceVersion and payload keys validated against it by exact set equality and a contract test forcing a SCHEMA_VERSION change whenever the registry changes \u2014 i.e. the store CAN be checked against the domain, which is exactly what this issue demanded (\"stating it where the store can be checked against it\").\n\nTHE SECOND-ORDER CONSEQUENCES this issue listed are each settled: (a) Retirement attestation \u2014 dissolved, not answered: \u00a71 keeps `retireSources` DELETED (\"quiet sources keep their key at a non-advancing watermark; surface-set changes are schema bumps\", doc lines 32-33). There is no retirement event to attest for either providers or projects. (b) Key-set size / one-sided-key wedge frequency \u2014 the set is exactly six and fixed per schema version; wedges from key churn cannot occur at all (a missing key is a schema violation, not a wedge). (c) `provenance.sourceTimestamps` keying \u2014 same domain by intent; residual below.\n\nTHE T-011 ADDENDUM (2026-08-04) is also dissolved: the scope item it blocked was the retirement attestation caller and the bootstrap-replacement's `sourceVersion` truthfulness. With `retireSources` deleted and the key domain fixed at six registry surfaces, the retirement path no longer exists to implement, and docs/t011-headless-service-plan.md \u00a712 item 5 was rewritten 2026-08-05 (ISS-075 now narrows T-011 to one bootstrap publish rather than blocking it). T-011 was unparked 2026-08-05.\n\nRESIDUAL, owned by T-025 (live, parked on ISS-090/091): the executable registry and exact-set-equality checks are T-025 items 1 and 3 (T-025.json description, re-specified 2026-08-04: \"sourceVersion must be exact-set-equal to the ingestion-surface registry keys (\u00a71)\"). Until that lands, `compareSourceVersions` (dominance.ts:161-190) still treats keys as opaque and `assertGenerationInvariants` validates `provenance.sourceTimestamps` keys (store.ts:1064-1073, current tree) without a domain check \u2014 the DEFINITION is decided; its ENFORCEMENT is T-025's. Note also that one of the six surfaces (claude-transcript-hours) has an open derivability defect, ISS-090, and claude-blocks has an open disappearance-policy defect, ISS-091 \u2014 those block T-025's implementation of the registry but do not reopen the key-domain question: the domain is decided regardless of how surface 6 is computed.\n\nNote on the issue's own recommendation: it recommended provider-level keys; the owner decided surface-level keys instead, which strictly refines it \u2014 provider-shaped surfaces exist (claude-blocks, codex-sessions) so the sub-day-witness argument survives, while the recommendation's accepted cost (\"gives up per-project regression detection\") is NOT paid: \u00a73's coverage attestation runs over per-row identities including (project, date) for claude-instances, so a single project's data going backwards inside an advancing surface refuses as `coverage-regression`.\n\n---\nEVIDENCE INDEX (verified against the tree at f44ff46 before closing):\ndocs/snapshot-facts-and-watermarks.md:14 (\"\u00a71 A source is an INGESTION SURFACE (answers ISS-082)\"); :19-24 (the six surfaces with invocations); :28-33 (registry fixed per SCHEMA_VERSION, exact-set validation, contract test, retireSources stays deleted); :57-58 (row identities incl. \"instances (project, date)\"). .story/notes/N-009.json content: \"The three root issues (ISS-081/082/087) are answered... sourceVersion keys are SIX ingestion surfaces\". .story/tickets/T-025.json description items 1 and 3 (exact-set-equal to registry keys; six surfaces named) and park.reason (\"Remaining in this ticket: the v2 contract work (surfaces registry, coverage policies, SnapshotPayloadV1) gated on ISS-090/ISS-091\"). Tree spot-checks: src/ccusage.ts:68-79 (instances), :82-93 (dailyAll), :96-100 (blocks), :103-110 (codexSessionRaw), :113-124 (codexDailyRaw) match the registry's five ccusage invocations; src/snapshot/types.ts:310 sourceVersion: Record<string, number> (issue cited :303 \u2014 drifted by working-tree edits, claim itself still true); src/snapshot/dominance.ts:161-190 compareSourceVersions opaque keys (confirmed); src/snapshot/store.ts:1064-1073 sourceTimestamps validated without domain check (confirmed). ISS-090/ISS-091 confirmed open.",
  "resolvedDate": "2026-08-05",
  "severity": "critical",
  "status": "resolved",
  "title": "sourceVersion's key domain is undefined \u2014 \"per-source\" is unspecifiable until someone decides whether a source is a provider, a project, or a transcript root"
}