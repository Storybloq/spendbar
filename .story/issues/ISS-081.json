{
  "components": [
    "snapshot",
    "ccusage"
  ],
  "createdBy": "T-025 plan round 1 (self-audit under RULES.md L-006)",
  "dedupeKey": "t025-plan-r1:watermark-intraday-witness",
  "discoveredDate": "2026-08-04",
  "displayId": "ISS-081",
  "id": "ISS-081",
  "impact": "BLOCKED ON ISS-082, WHICH MUST BE ANSWERED FIRST. Plan review round 3 established that this issue's central term - \"per-source\" - is undefined, and that the answer changes this issue's conclusion rather than merely informing it. sourceVersion is Record<string, number> and nothing binds the key domain (see ISS-082). Read that first; what follows is correct only once \"source\" means something.\n\nWHAT IS ESTABLISHED REGARDLESS OF ISS-082. The only per-entity ccusage surface, `claude daily --instances` (src/ccusage.ts:68-78), is DAY-granular: rows carry `date`, `totalCost`, `totalTokens`, four optional counters, `modelBreakdowns` (src/json.ts:327-331). Generic `daily` uses `period`, same granularity. A day-granular watermark does not advance within a day, so if the key domain resolves to projects or instances, then no intra-day refresh can ever publish - defeating T-012's fast tier and the product - and N-008 1b would report \"found nothing new\" for a run that found real new spend, which is the exact freshness lie 1b exists to prevent.\n\nWHAT IS NOT ESTABLISHED. That no per-source sub-day signal exists. `ccusage blocks --json` (src/ccusage.ts:96) rows carry `startTime`, `endTime` and `actualEndTime` as ISO instants with ms precision (tests/fake_ccusage.py:67-70), consumed at renderers.ts:397-399, reached by the live `blocks` subcommand (main.ts:70,122,146). The rows have no source key, but the COMMAND is Claude-only - so under provider-level keys that timestamp IS a per-source sub-day signal for Claude, and this issue narrows to Codex. Under project-level keys it is global and this issue stands at full width. Codex `sessionFile` (ROLLOUT_RE, codex.ts:59-62) embeds a time but it is a session START time, which a growing session never advances, so it does not witness newest activity under any key domain.\n\nHISTORY OF THIS FILING, kept because the pattern matters more than the conclusion. Version 1 claimed \"no row in any ccusage surface carries a sub-day event timestamp\" - FALSE, refuted by review round 2, which found blocks. Version 2 claimed \"no PER-SOURCE sub-day signal exists\" - NOT ESTABLISHED, refuted by review round 3, which found that the term was undefined. Both times the error was the same: I audited what I was already looking at, found what I expected, and stopped. ISS-072 was withdrawn for that pattern earlier in this arc. That is three instances, and the lesson is not \"check ccusage surfaces\" but \"an audit that terminates when it finds a confirming instance has not been performed\".\n\nCANDIDATE RESOLUTIONS, once ISS-082 is answered:\n(a) Ordered composite per-source version, e.g. (newest day, tokens on that newest day) - monotone and pruning-robust, since pruning never touches the newest day. Costs: sourceVersion is Record<string, number>, so the pair needs a checked safe-integer encoding, or the type and compareSourceVersions both change on shipped T-010. Splitting across two keys does NOT work: at day rollover the date component rises while the token component resets, giving anyGreater && anyLess = incomparable (dominance.ts:184).\n(b) Keep day-granular watermarks and REDEFINE \"unchanged\" as candidate-payload-identical-to-live rather than watermark-equal, letting an equal watermark with a differing payload publish. Weakens the gate to refusing only regression.\n(c) Accept day-granular publishing for v0.2 and move intra-day freshness to the v0.5 owned-parsing ticket - i.e. state plainly that the v0.2 service updates spend once per day.\n(d) Per-source day watermarks plus the global blocks.actualEndTime as an intra-day tiebreak. WITHDRAWN AS PROPOSED, and the reason is worth keeping: review round 3 showed it commits this arc's own substitution. To claim the payload REFLECTS activity through blocks.actualEndTime, blocks must be invoked BEFORE the payload-producing command; invoked after, it proves only that blocks SAW activity, not that the computed payload included it - bytes-available-as-bytes-consumed wearing a new hat, inside the fix proposed for it. It was also underspecified on persistence, regression handling, interaction with retirement and equality, and invocation order, and it cannot unblock a growing Codex session when the Claude tiebreak is unchanged. Revivable only with a strict `blocks` -> payload ordering contract and a full composite comparison table.\n\nIf ISS-082 resolves to PROVIDER keys, a fifth option opens: Claude uses blocks.actualEndTime as a genuine per-source sub-day watermark while Codex uses day granularity, with the asymmetry documented rather than hidden. That is the option this issue could not see while \"source\" was undefined.",
  "location": [
    "src/snapshot/dominance.ts:161-190",
    "src/snapshot/store.ts:2180-2192",
    "src/ccusage.ts:63-78",
    "src/json.ts:327-331"
  ],
  "phase": null,
  "relatedTickets": [
    "T-011",
    "T-012",
    "T-013",
    "T-025"
  ],
  "resolution": "RESOLVED 2026-08-05 \u2014 ANSWERED by N-009 (.story/notes/N-009.json) via docs/snapshot-facts-and-watermarks.md v2 \u00a72/\u00a73. \u00a72's heading names it: \"Watermarks attest RECENCY only (answers ISS-081's witness half)\".\n\nWHY THE PROBLEM DISSOLVES RATHER THAN GETTING THE MISSING WITNESS. This issue's harm chain was: per-source watermark cannot advance intra-day \u2192 \"no intra-day refresh can ever publish \u2014 defeating T-012's fast tier\" \u2192 N-008 1b reports \"found nothing new\" for real new spend. N-009 breaks the MIDDLE link. Under \u00a72, \"a watermark alone never authorizes replacement\" (doc :50), and \u00a73 defines a `refreshed` outcome: \"coverage passes + watermark equal + any content update \u2192 {status:\\\"refreshed\\\"}\" (doc :80). New spend today changes content at an existing row identity \u2014 instances (project, date), all-daily (date), blocks (startTime) \u2014 and \u00a73 explicitly allows \"Content updates at an existing identity... This is where growing active blocks, growing live codex sessions, and pricing recomputes land\" (doc :63-65). So an intra-day refresh publishes WITHOUT any watermark advancing, and the N-008 1b freshness lie cannot occur: real new spend yields `refreshed`, never `unchanged`. Nothing in the decided model needs a per-source sub-day activity time.\n\nAND THE WITNESS PARTLY EXISTS ANYWAY, exactly as this issue's closing paragraph foresaw: with ISS-082 resolved to ingestion-surface keys (provider-shaped for two surfaces), \u00a72 gives claude-blocks a genuine sub-day watermark \u2014 \"max(actualEndTime | endTime | startTime) over NON-GAP rows\" (doc :41-43) \u2014 and codex-sessions gets \"max validated rollout-filename start instant\", which a NEW session advances intra-day while a GROWING session lands as a \u00a73 content update at its rollout-id identity. The day-floor surfaces (claude-instances, all-daily, codex-daily) keep day-granular watermarks with the asymmetry documented, not hidden (doc :46). This is the \"fifth option\" the issue said it could not see while \"source\" was undefined, generalized to six surfaces.\n\nDISPOSITION OF THE FOUR CANDIDATE RESOLUTIONS: (a) composite versions \u2014 not needed, not adopted. (b) is essentially what was decided, HARDENED against the weakness the issue identified: instead of \"candidate-payload-identical-to-live\" (v1's digest trick, REJECTED in codex review 019fcfc4 as able to silently publish data loss), \u00a73 attests coverage over stable row identities, refusing interior gaps / leading-edge loss / recent-rows-gone as typed `coverage-regression` while allowing additions and in-place growth \u2014 stronger, not weaker, than the watermark gate. (c) not needed \u2014 intra-day publishing works via `refreshed`. (d) stays withdrawn and its hazard is moot: blocks' timestamp is now a PER-SURFACE watermark over its OWN normalized rows, not a global tiebreak asserted over other surfaces' payloads, so the blocks-before-payload ordering contract it demanded never arises.\n\nMEASURED CLAIMS re-checked against the current tree, all still hold: `claude daily --instances` is day-granular (src/ccusage.ts:68-79); `ccusage blocks --json` exists with no per-project key (src/ccusage.ts:96-100); codex rollout filenames embed a session START instant (src/codex.ts:59-62 ROLLOUT_RE; the doc agrees and handles it \u2014 start instants feed the watermark, growth feeds content updates). No landed code contradicts them.\n\nRESIDUAL, owned by T-025 (live): item 3 of T-025's re-specified description implements the registry watermark functions, the four-outcome contract, and the coverage policies, with tests observing each outcome. T-025 is parked on ISS-090 (claude-transcript-hours derivability) and ISS-091 (claude-blocks disappearance policy) \u2014 both are implementation defects in OTHER parts of the decided model and neither reopens this issue's question of intra-day watermark advance. The audit-termination lesson recorded in this issue's history (three instances of stopping at a confirming find) is preserved by this closure, not erased; it is independently held as a ledger lesson.\n\n---\nEVIDENCE INDEX (verified against the tree at f44ff46 before closing):\ndocs/snapshot-facts-and-watermarks.md:34 (\"\u00a72 Watermarks attest RECENCY only (answers ISS-081's witness half)\"); :41-43 (claude-blocks watermark = max(actualEndTime|endTime|startTime), non-gap, defined fallback per finding 2); :44-45 (codex-sessions = max rollout start instant, undated sessions excluded from watermark, included in rows); :46 (day-floor surfaces); :49-50 (\"a watermark alone never authorizes replacement\"); :62-65 (content updates at existing identity allowed \u2014 growing blocks/sessions); :78-82 (four outcomes incl. refreshed); :9-13 (core correction: recency vs completeness; v1 digest rejected). Tree spot-checks: src/ccusage.ts:68-79 (instances day-granular, matches issue), :96-100 (blocks), src/codex.ts:59-62 (ROLLOUT_RE start-timestamp filename, matches issue's session-start claim), src/snapshot/dominance.ts:174-190 (anyGreater&&anyLess \u2192 incomparable, matching the issue's rollover analysis at :184 \u2014 untouched, but no longer the sole gate). .story/tickets/T-025.json description item 3 (registry, watermark functions, four outcomes, coverage tests) and park.reason (v2 contract work remains in T-025, gated on ISS-090/091). ISS-090/ISS-091 confirmed open and about transcript-hours derivability and blocks disappearance policy respectively \u2014 neither about intra-day watermark advance.",
  "resolvedDate": "2026-08-05",
  "severity": "critical",
  "status": "resolved",
  "title": "No ccusage surface gives a PER-SOURCE sub-day activity time: a per-source watermark cannot advance intra-day, and the only sub-day signals are global or session-start"
}